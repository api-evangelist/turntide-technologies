generated: '2026-08-02'
method: searched
status: published
source: https://turntide.com/.well-known/oauth-protected-resource
server:
  name: turntide-wordpress-mcp
  transport: http
  url: https://turntide.com/wp-json/mcp/mcp-oauth-server
  implementation: WordPress MCP Adapter (WordPress `mcp` REST namespace)
  discovered_via:
  - https://turntide.com/wp-json/ (route index, namespace "mcp")
  - https://turntide.com/.well-known/oauth-protected-resource
alternate_endpoints:
- url: https://turntide.com/wp-json/mcp/mcp-adapter-default-server
  auth: WordPress cookie/application-password session
  probe:
    method: POST tools/list
    http_status: 401
    body: '{"code":"rest_forbidden","message":"Sorry, you are not allowed to do that.","data":{"status":401}}'
authentication:
  type: oauth2
  scheme: OAuth 2.1 authorization code + PKCE (S256)
  authorization_server: https://turntide.com
  authorization_endpoint: https://turntide.com/oauth/authorize
  token_endpoint: https://turntide.com/oauth/token
  revocation_endpoint: https://turntide.com/oauth/revoke
  scopes:
  - mcp
  dynamic_client_registration: false
  client_id_metadata_document_supported: true
  bearer_methods_supported:
  - header
probe:
  method: POST tools/list
  url: https://turntide.com/wp-json/mcp/mcp-oauth-server
  http_status: 401
  body: '{"code":"mcp_unauthorized","message":"MCP authentication required.","data":{"status":401}}'
  fetched: '2026-08-02'
tools: []
tools_note: The live tool list is auth-gated. An anonymous JSON-RPC tools/list returns HTTP 401 mcp_unauthorized,
  so no tool names or inputSchemas could be observed and none are asserted here. Turntide publishes no
  llms.txt or MCP documentation listing the tool set, so there is no published fallback list to record
  either. Enumerating the real tools requires an authenticated OAuth introspection pass with the "mcp"
  scope.
x-notes: This MCP server is a WordPress MCP Adapter installed on the turntide.com corporate website. It
  projects the site's registered WordPress Abilities (the wp-abilities/v1 REST namespace, also present
  in the route index) as MCP tools. It is a content/CMS agent surface — it is not an agent interface to
  Turntide's motor, power electronics, energy storage or thermal products, for which Turntide publishes
  no API.
deployment:
  mode: remote
  endpoint: https://turntide.com/wp-json/mcp/mcp-oauth-server
  verified: probed
  probe: gated
  checked: '2026-08-12'
  source: catalog MCP census
